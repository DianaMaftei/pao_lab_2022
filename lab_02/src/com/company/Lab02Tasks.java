package com.company;

public class Lab02Tasks {
    // TODO CLASSES

    // create class Animal -> add one numeric field and one alphanumeric field
    // add getters and setters
    // create constructor with one field and an overloaded (discuss default constructor) with both - use this

    // create two Animal objects -> default constructor and overloaded constructor
    // display info using the getters from the class

    // create two methods: makeSound and move & call them for the above created objects

    // create two classes: Bird and Cat that extend the Animal class
    // override makesSound and move
    // create enum with CatBreeds and add to Cat class

    // create a Cat and a Bird object
    // call overridden methods
    // get fields from these classes, check the enum

    // create two birds with the same fields & create a 3rd bird and assign it the value of the 1st one
    // check equality between the 3
    // override equals and hashcode & check equality again

    // override toString method for one of the classes and call it on one of the objects


    // TODO FINAL

    // create a final variable, try to reassign a value
    // create a final method in the animal class, try to override in child class
    // create a final class, try to extend it


    // TODO STATIC

    // create static field
    // create static method
    // create static block
    // create constant

    // TODO order of initialization -> try at home -> https://farenda.com/java/java-initialization-order/
}
